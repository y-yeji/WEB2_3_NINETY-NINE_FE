export interface ShowInfo {
  posterUrl: string;
  title: string;
  audience: string;
  period: string;
  location: string;
  times: string;
  links: string;
  price: string;
  reviewCount: number;
  isBookmarked: boolean;
}
